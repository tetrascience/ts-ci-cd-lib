name: Coverage Report

on:
  workflow_call:
    inputs:
      coverage-artifact-name:
        description: "Name of the artifact containing coverage-summary.json and coverage-final.json"
        type: string
        default: "coverage"
      base-coverage-artifact-name:
        description: "Name of the artifact containing base branch coverage-summary.json"
        type: string
        default: "base-coverage"
      caller-workflow:
        description: "Filename of the caller workflow (for cross-run artifact download)"
        type: string
        required: true
      new-code-threshold:
        description: "Minimum statement coverage % for changed source files (0 to disable)"
        type: number
        default: 95
      source-file-pattern:
        description: "Grep regex for source files to enforce new-code threshold on"
        type: string
        default: '^src/.*\.(ts|tsx)$'
      source-file-exclude:
        description: "Grep regex for files to exclude from new-code threshold"
        type: string
        default: '\.(test|spec|stories)\.(ts|tsx)$'

jobs:
  coverage-report:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Download coverage artifacts
        uses: actions/download-artifact@fa0a91b85d4f404e444e00e005971372dc801d16 # v4
        with:
          name: ${{ inputs.coverage-artifact-name }}
          path: coverage/

      - name: Download base coverage from main
        uses: dawidd6/action-download-artifact@bf251b5aa9c2f7eeb574a96ee720e24f801b7c11 # v6
        with:
          name: ${{ inputs.base-coverage-artifact-name }}
          branch: main
          workflow: ${{ inputs.caller-workflow }}
          path: base-coverage/
        continue-on-error: true
        id: base-download

      - name: Report coverage on PR
        uses: davelosert/vitest-coverage-report-action@2500dafcee7dd64f85ab689c0b83798a8359770e # v2
        with:
          json-summary-path: coverage/coverage-summary.json
          json-final-path: coverage/coverage-final.json
          json-summary-compare-path: ${{ steps.base-download.outcome == 'success' && 'base-coverage/coverage-summary.json' || '' }}

      - name: Enforce coverage rules
        if: steps.base-download.outcome == 'success'
        env:
          GH_TOKEN: ${{ github.token }}
          NEW_CODE_THRESHOLD: ${{ inputs.new-code-threshold }}
          SOURCE_FILE_PATTERN: ${{ inputs.source-file-pattern }}
          SOURCE_FILE_EXCLUDE: ${{ inputs.source-file-exclude }}
        run: |
          FAILED=0

          # 1. Check overall coverage didn't decrease
          BASE_LINES=$(jq '.total.lines.pct' base-coverage/coverage-summary.json)
          PR_LINES=$(jq '.total.lines.pct' coverage/coverage-summary.json)

          echo "Base coverage: ${BASE_LINES}%"
          echo "PR coverage:   ${PR_LINES}%"

          if [ "$(echo "$PR_LINES < $BASE_LINES" | bc -l)" -eq 1 ]; then
            echo "::error::Overall line coverage decreased from ${BASE_LINES}% to ${PR_LINES}%"
            FAILED=1
          fi

          # 2. Check changed source files meet new-code threshold
          if [ "$NEW_CODE_THRESHOLD" -gt 0 ]; then
            CHANGED_FILES=$(gh api repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }}/files --jq '.[].filename' \
              | grep -E "$SOURCE_FILE_PATTERN" \
              | grep -vE "$SOURCE_FILE_EXCLUDE" \
              | grep -vE '/index\.(ts|tsx)$') || true

            for file in $CHANGED_FILES; do
              PCT=$(jq -r --arg f "$file" '
                to_entries[]
                | select(.key | endswith($f))
                | .value.s
                | to_entries
                | if length == 0 then 100
                  else ([.[].value] | (map(select(. > 0)) | length) / length * 100)
                  end
              ' coverage/coverage-final.json 2>/dev/null)

              if [ -n "$PCT" ] && [ "$PCT" != "null" ]; then
                ROUNDED=$(echo "$PCT" | awk '{printf "%d", $1}')
                if [ "$ROUNDED" -lt "$NEW_CODE_THRESHOLD" ]; then
                  echo "::warning file=$file::Coverage is ${ROUNDED}% (required: ${NEW_CODE_THRESHOLD}%)"
                  FAILED=1
                fi
              fi
            done
          fi

          if [ "$FAILED" -eq 1 ]; then
            echo "::error::Coverage checks failed. See above for details."
            exit 1
          fi

          echo "All coverage checks passed."
