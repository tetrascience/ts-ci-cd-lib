name: Upload Coverage Artifacts
description: >
  Upload coverage artifacts for PR reporting and base branch comparison.
  Uploads the full coverage directory as an artifact on every run, and
  uploads coverage-summary.json as a separate base-coverage artifact
  on main branch pushes (for PR comparison).

inputs:
  coverage-path:
    description: "Path to the coverage output directory"
    default: "coverage/"
  coverage-artifact-name:
    description: "Name of the coverage artifact"
    default: "coverage"
  base-coverage-artifact-name:
    description: "Name of the base coverage artifact (uploaded on main only)"
    default: "base-coverage"
  retention-days:
    description: "Number of days to retain the coverage artifact"
    default: "30"
  base-retention-days:
    description: "Number of days to retain the base coverage artifact"
    default: "90"

runs:
  using: composite
  steps:
    - name: Upload coverage artifacts
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: ${{ inputs.coverage-artifact-name }}
        path: ${{ inputs.coverage-path }}
        retention-days: ${{ inputs.retention-days }}

    - name: Upload base coverage for PR comparison
      if: github.ref == 'refs/heads/main'
      uses: actions/upload-artifact@v4
      with:
        name: ${{ inputs.base-coverage-artifact-name }}
        path: ${{ inputs.coverage-path }}coverage-summary.json
        retention-days: ${{ inputs.base-retention-days }}
        overwrite: true
